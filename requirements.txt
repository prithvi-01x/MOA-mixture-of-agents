# Python 3.11.9
#
# Install all (runtime + dev):
#   pip install -r requirements.txt
#
# For production only, comment out the "Dev / Testing" block below.

# ── Runtime ────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # ASGI server (standard extras: websockets, watchfiles)
pydantic>=2.7.0                # request/response models inside FastAPI routers
aiosqlite>=0.20.0              # async SQLite driver used in database.py
httpx>=0.27.0                  # async HTTP client used by all three providers
python-dotenv>=1.0.0           # .env loading in config.py
loguru>=0.7.0                  # structured logging in logger.py

# ── Dev / Testing ──────────────────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0         # async test support (conftest.py + all test files)
# httpx is already listed above; AsyncClient + ASGITransport are part of the same package
